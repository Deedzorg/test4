{
  "name": "infrastry-gauntlet",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Evidence-driven deployment observability harness for testing Infrastry automation.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node boot.mjs",
    "check": "node --check boot.mjs && node --check server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.0.0",
    "ws": "^8.0.0"
  }
}
